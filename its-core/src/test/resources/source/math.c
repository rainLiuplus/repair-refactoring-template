#include <math.h>
#include <stdlib.h>

int main() {
    double x = 2.7, y = -1.3;
    double x_abs = abs(x);
    double y_abs = abs(y);
    double x_floor = floor(x);
    double y_floor = floor(y);
    double x_ceil = ceil(x);
    double y_ceil = ceil(y);
}